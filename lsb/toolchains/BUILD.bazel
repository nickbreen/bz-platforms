load(":defs.bzl", "lsb_toolchain")

package(default_visibility = ["//visibility:public"])

lsb_toolchain(
    name = "redhat",
    src = "/etc/redhat-release",
    cmd = "cp $1 $2",
)

lsb_toolchain(
    name = "systemd",
    src = "/etc/os-release",
    cmd = """. $1; echo ${!3} > $2""",
)
